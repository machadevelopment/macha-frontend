/**
 * Destino de los CTAs "Solicitar demo" / "Contacto" de la landing.
 *
 * Antes era un `mailto:` (pedido de Keneth). Ahora es el ancla del formulario en la misma
 * página (pedido de Jose, 2026-08-21). El correo sigue visible en el footer como canal
 * paralelo — no se esconde.
 */
export const ANCLA_DEMO = '#demo';

/** Correo público de contacto. También es el destinatario del aviso del formulario. */
export const CORREO_DEMO = 'contact@machafinance.com';
