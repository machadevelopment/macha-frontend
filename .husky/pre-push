# CU-868khvgpy — CAUSA CONFIRMADA (2026-07-30). El hook nunca fue el culpable.
#
# Síntoma: `git push` en este repo se colgaba indefinidamente (abortado a los 2 y a los
# 10 minutos). Dos hipótesis previas quedaron descartadas:
#   1. "`next lint` espera stdin (wizard interactivo de ESLint)". Falso: `bun run lint` y
#      `bun test` corridos con stdin conectado a un pipe que nunca cierra terminaron en
#      verde y en tiempo normal.
#   2. "Es el hook". Falso: `git push --no-verify` también se colgaba.
#
# Causa real, vista con `GIT_TRACE=1`: el `~/.gitconfig` de la máquina tiene
# `credential.username = kenethruiz20`. Git fija ese usuario antes de consultar helpers,
# así que `gh auth git-credential` — que solo tiene credencial para la cuenta
# `machadevelopment` — no responde para ese usuario, git cae al askpass de VS Code
# (`.../extensions/git/dist/askpass.sh`), este lanza un Electron pidiendo contraseña por
# GUI, y el push espera para siempre una respuesta que nadie va a dar.
#
# Arreglo (por máquina, no versionable — el `.gitconfig` global no vive en el repo):
#
#     git config --local credential.username machadevelopment
#
# Ya aplicado en este clon. Hay que repetirlo en cada clon nuevo y en macha-backend, o
# quitar `credential.username` del `~/.gitconfig` global para arreglarlo de una vez.
# Con eso el push completa con el hook activo, en tiempo normal.
#
# El `< /dev/null` de abajo se deja como defensa barata (ningún comando lee stdin y así
# no puede quedarse esperándolo), pero NO es lo que arregló el cuelgue.
#
# `build` no se corre aquí a propósito (criterio 4 del ticket): `next build` tarda más
# que los otros tres juntos y el gate de CI ya lo cubre desde CU-868khttg2
# (.github/workflows/ci.yml). El hook es el bucle rápido; CI es la red final.
bun run typecheck < /dev/null && bun run lint < /dev/null && bun test < /dev/null
